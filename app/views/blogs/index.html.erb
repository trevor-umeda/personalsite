<div id ="blog_description">
   <%= @tag.description %>
</div>
<div id ="tag form">
  <% if user_signed_in? %>
  <%= form_for @tag, :remote => true do |tag|%>
        <div class="field">
          <%= tag.label :description %><br />
          <%= tag.text_area :description, :rows => 2 %>
        </div>

        <div class="actions">
          <%= tag.submit %>
        </div>

  <% end %>
      <% end %>
</div>
<div id="blog_list">
<%= render @blogs %>
</div>
<br>
<% if current_user %>
<%= form_for @blog do |f| %>
  <% if @blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @blog.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class = "field">
     <%= f.label :title %><br />
     <%= f.text_field :title%>

   </div>
  <div class="field">

    <%= f.label :post %><br />
    <%= f.text_area :post, :rows => 9 %>
  </div>
  <div class="field">
    <%= f.label :tag %><br />
    <input id="tag_name" name="tag_name" type="text" /> </br>

    <%= f.collection_select(:tag_id, Tag.all, :id,:name, {:prompt => 'Select Tag'},:style => "width: 175px") %>


  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<% end %>


